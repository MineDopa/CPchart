# CPC 填写说明

> 适用范围：CPChart v0.15.0 起（块语法）
> 相关文档：同目录 `CPC-AI生成指引.md`（给 AI 用的生成指引）
> 性质：用户文档

---

## 一、本文用途

本文说明如何使用 CPC 文本格式填写角色关系表。用户按本文书写文本，工具读取后生成关系图。

本文仅覆盖当前版本支持的范围。

---

## 二、最小示例

```
#标题{名字：我的CP图；!填表人：你的名字}

.布局{
  0：甲
  1：乙，丙
}

@关系{
  爱情：甲*乙
}

!喜好 原子{
  本命：乙
}

!喜好 关系{
  很喜欢：甲*乙
}

.图例{
  "关系类型": ["爱情"],
  "用户喜好": ["本命", "很喜欢"],
  "情感指向": ["情感主体"->"情感对象"]
}
```

示例含义：

| 项 | 内容 |
|---|---|
| 图标题 | 我的CP图 |
| 填表人 | 你的名字 |
| 布局 | 圆心为「甲」；第 1 圈为「乙」「丙」 |
| 关系 | 甲与乙之间存在「爱情」关系 |
| 喜好（角色） | 填表人对角色「乙」的喜好为「本命」 |
| 喜好（关系） | 填表人对「甲—乙」这一对的喜好为「很喜欢」 |
| 图例 | 分别说明关系类型、用户喜好、情感指向 |

> 最小可用文件只需 `#标题{}` 与 `.布局{}` 两块，其余均可省略。

---

## 三、块结构

| 块 | 用途 | 本版 |
|---|---|---|
| `#标题{}` | 图标题与填表人 | 支持 |
| `.布局{}` | 圈层与节点位置 | 支持 |
| `@关系{}` | 角色之间的关系 | 支持 |
| `!喜好 原子{}` | 填表人对角色的喜好 | 支持 |
| `!喜好 关系{}` | 填表人对关系的喜好 | 支持 |
| `.样式{}` | 颜色等视觉配置 | 支持（详见 4.6） |
| `.图例{}` | 图例内容 | 支持 |
| `.时间轴{}` | 时间关键帧 | 不支持 |
| `@原子{}` `@组合{}` `!喜好 组合{}` | 超点与组合 | 不支持 |

> 不支持的块**不会导致报错**，其内容会被跳过。

---

## 四、填写规则

### 4.1 `#标题{}` —— 图标题与填表人

```
#标题{名字：我的CP图；!填表人：你的名字}
```

- 首行若不写字段名，则该值视为图标题
- `名字`：图标题
- `!填表人`：填表人姓名。字段名前缀 `!`，表示该字段由人填写
- `作品`：可写成 `IP > 作品名` 的形式
- `时间`：建表时间
- 字段之间以 `；` 或换行分隔，字段名与值之间以 `：` 分隔

### 4.2 `.布局{}` —— 圈层与节点位置

```
.布局{
  0：甲
  1：乙，丙
  2[8, 270px]：丁，戊，己
}
```

- 行首数字为圈层序号，`0` 为圆心，`1` 为最内圈
- 冒号后为该圈层上的角色，以 `，` 分隔
- 圈层后的 `[槽位数, 半径]` 为可选项：
  - `[8, 270px]` —— 槽位 8、半径 270px
  - `[8]` —— 只给槽位；`[270px]` —— 只给半径
  - **区分依据是「有无单位」**：裸数字是槽位，带 `px` 等单位的是半径
- 槽位数表示该圈预留的位置数，应不小于该圈人数；省略则按实际人数
- 省略半径则按默认等距：第 1 圈 150px，此后每圈 +120px

### 4.3 `@关系{}` —— 角色之间的关系

```
@关系{
  爱情：甲 -> 乙
  友情：乙 * 丙
  宿敌：丁 -> 戊
}
```

- 冒号前为关系词，可使用「爱情 / 友情 / 亲情 / QPR」，亦可自定
- 冒号后为关系对象，方向符号三种：
  | 写法 | 含义 |
  |---|---|
  | `甲*乙` | 两者之间的关系，不区分方向 |
  | `甲->乙` | 有方向，由甲指向乙 |
  | `甲<->乙` | 双向 |
- 同一关系词下可书写多条，以 `，` 分隔
- 同一对角色可出现在多个关系词下（如既是「亲情」又是「敌对」）

### 4.4 `!喜好 原子{}` —— 填表人对角色的喜好

```
!喜好 原子{
  本命：乙
  很喜欢：丙，丁
}
```

- 本块表示填表人**对角色本人**的喜好，会体现在角色的圆圈颜色上
- 冒号后**只能是人名**，不可出现 `*` 或 `->`
- 喜好词可使用「本命 / 很喜欢 / 路好 / 不吃」，亦可自定
- 一个角色只能有一种喜好

### 4.5 `!喜好 关系{}` —— 填表人对关系的喜好

```
!喜好 关系{
  很喜欢：甲*乙
  雷：丁<->戊
}
```

- 本块记录**填表人**对关系的喜好，与 4.3 的客观关系相互独立
- 冒号后**必须是关系**（含 `*` 或 `->`），不可只写人名
- 同一对角色可同时出现于 4.3 与 4.5：前者说明「是什么关系」，后者说明「填表人的喜好程度」

### 4.6 `.样式{}` —— 颜色等视觉配置

```
.样式{
  爱情：#E24B4A
  宿敌：relation, #4B8BE2, 2px, 虚线
}
```

- 格式：`词：颜色 [, 逻辑, 线宽, 描边]`
- **只需书写与默认不同的部分**：只改颜色就只写颜色，其余字段自动沿用默认值
- 自定关系词**必须**在本块出现一次（至少给出颜色）
- 逻辑二选一：`relation`（关系层）/ `favor`（喜好层）。省略时按该词的用法自动判定
- 描边取值：`实线 / 虚线 / 点线 / 双线`
- 本版本**仅颜色生效**；线宽与描边会被保存，但不改变当前画面

### 4.7 `.图例{}` —— 图例

```
.图例{
  "关系类型": ["爱情"],
  "用户喜好": ["本命"],
  "情感指向": ["情感主体"->"情感对象"]
}
```

图例分三组：

| 组名 | 对应来源 | 说明 |
|---|---|---|
| `关系类型` | 4.3 `@关系{}` | 关系层的词 |
| `用户喜好` | 4.4 / 4.5 `!喜好` | 喜好层的词 |
| `情感指向` | 箭头 | 箭头由「情感主体」指向「情感对象」 |

- 各组值为词列表，以引号包裹、逗号分隔
- `情感指向` 为单条箭头说明
- 本版本图例不显示三行分组标题，仅显示词条本身
- 省略本块时，前两组按实际用到的词自动生成

---

## 五、本版支持范围

**支持**

- 图标题、填表人、作品、建表时间
- 圈层与节点位置（圆心、圈号、槽位、半径）
- 角色之间的关系（含自定关系词），方向与双向写法
- 填表人对角色、对角色的喜好
- 线条颜色
- 图例（三组均支持）
- 时间：可标注角色出现的时间点，且仅有一条时间线

**不支持**

- 角色嵌套（一人含于另一人之内）
- 新增时间线或分支
- 线条粗细与线型（虚线、点线等）——可书写并保存，但不影响画面
- 时间轴、原子、组合三类块

---

## 六、书写约定

- 中文标点与英文标点均可使用，工具会自动统一（全角逗号、分号、冒号、引号均被接受）
- 各类箭头写法均可：`→` `➡` `->` 等价；`←` `<-` 等价；`↔` `<->` 等价；`×` `✖` `*` 等价
- 引号内的内容不参与标点转换
- 行尾分号可省略，换行即视为分隔
- 建议书写顺序：`#标题{}` → `.布局{}` → `@关系{}` → `!喜好 原子{}` → `!喜好 关系{}` → `.样式{}` → `.图例{}`
