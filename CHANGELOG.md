# 版本更新说明 · CPchart

> 与「关于」页更新日志同源，按大版本归档。本文件遵循**约定式提交规范**（Conventional Commits）。
> 格式：`类型(范围): 说明`。类型速查：`feat` 新增 · `fix` 修复 · `style` 样式 · `perf` 优化 · `refactor` 重构 · `docs` 文档 · `chore` 杂项 · `revert` 回退。
> 类型↔中文对照详见 `约定式提交规范-中文对照.md`；给用户看的中文版公告见「关于」页与 `logbrief.md`。
> 一条改动记一个类型；混了两件事就拆两条。

## v0.15.4 · 2026-09-12（连线面板顺序 · 布局说明文案）

- `style(连线)`: 画板模式行上移 —— 连线/角色/删线 三个模式按钮与「批量编辑 / 清空」从笔刷行下方移到上方，先定模式再选笔刷
- `docs(布局)`: 布局说明文案展开 —— `#layoutHint` 由简写式（`拖拽角色=换圈/沿圈移动`）改为分行的完整句，并补明「槽位开启时槽位数量必须大于轨道上人物数量」
- `chore(排期)`: 撤销 / 重做连续操作记入排期 P6 —— 当前交互不便连续撤销

## v0.15.3 · 2026-09-12（全量覆盖 · 连线修复 · 描边滑条 · 重做禁用态）

- `feat(导入)`: 新增「全量覆盖」模式 —— 「逐条同步」Tab 更名「全量覆盖」：粘贴整份 CPC 块语法即整体替换画板（文本里没写的人物/连线会被删除），提交后立刻看到删除结果；`App.importCPC(text, "replace")` 走覆盖、省略第二参数仍为增量合并
- `fix(连线)`: 点起点→点终点（两段式）画不出线 —— `linkDrag` 抬起时无条件清空 `linkSource`，实际只支持按住拖拽；改为 tap 保留起点、再点第二节点落线，拖拽路径不变
- `fix(连线)`: 拖到目标节点松手命中失败 —— ghost 辅助线参与 `elementFromPoint` 命中；给 ghost 加 `pointer-events:none`
- `fix(样式)`: 「重做」按钮未变灰（disabled 但视觉无差别） —— `@keyframes radialPop` 末帧 `opacity:1` + `fill-mode:both` 覆盖了 `.radial-btn:disabled{opacity:.32}`；末帧改用 `var(--bt-op)`，禁用时置 `.32`
- `refactor(样式)`: 细线描边滑条改为控制外层白描边宽度 —— 范围 `2.2–10`（原 `1–6` 控内芯彩线宽），内芯固定 `2.2`，符合「描边」直觉
- `refactor(样式)`: 隐藏「细线虚线」开关 —— 虚线应随顶层图例走、而非全局开关，v1.0 暂不开放（保留代码，仅从面板移除入口）
- `fix(调色板)`: 前三个颜色修正 —— 第三色 `#fb3000` → `#ffb300`（对齐设计色卡 `#D32F2F / #F57C00 / #FFB300`）
- `docs(编辑器)`: 「全量覆盖」Tab 提示改用块语法示例并写明覆盖语义（原提示为已停用的五段制示例）

## v0.15.2 · 2026-09-11（调色板换新 · 箭头 SVG 化 · 返回钮修复）

- `style(调色板)`: 12 色色板换新 —— `PALETTE` 从旧 12 色（含黄/绿/蓝紫系）换成新 12 色；取色器自动生效，已存文档颜色不受影响（颜色存在文档自身）
- `fix(图例)`: 「情感指向」箭头在手机上显示为彩色 emoji —— `➡`(U+27A1) 被渲染成彩色 emoji；改为内联 SVG（界面）/ canvas 路径绘制（导出 PNG），不再依赖字体
- `fix(顶栏)`: 「返回画板」按钮尺寸偏大 —— 手机实测 25×25，大于声明的 2rem(20px)；锁定尺寸并加固内部图标约束，与同槽位帮助钮对齐
- `docs(归档)`: 旧格式/语言说明文档归档并修正过时引用 —— `格式说明v4/v5`、`数据格式语义定义-v2-cpc.md`、`web/CPC语言说明.md` 等移入 `_rubbish/`；修正被误标为「权威语义文档」的引用（现权威 = `格式说明v5.1.txt`）

## v0.15.1 · 2026-09-11（面板锚点 · 导入刷新 · AI 提示词改版）

- `fix(面板)`: 往下拖时「底边翘起」 —— 全开态余量堆在面板下方、锚点锁在顶边；改为锁底边
- `fix(导入)`: 完整数据替换成功后画布不重绘（看起来像「没覆盖」） —— 导入流程末尾补 `notifyChanged`，画布主动重绘
- `docs(AI 提示词)`: 改版 —— 布局不再要求 AI 写槽位与半径，统一「只写圈号与名单」；新增「只输出一个代码块、块外不写字」硬约束；样例整份换成最简形态

## v0.15.0 · 2026-09-11（填写格式换代：块语法）

> 权威语法文档：`格式说明v5.1.txt`（CPC 块语法的完整语法规范）。

- `feat(格式)`: 分段制改为块语法 —— 文本从 `#标题#` 一行行写，改为 `#标题{}` `.布局{}` `@关系{}` `!喜好{}` `.样式{}` `.图例{}` 一块块写，导入导出两端同步切换
- `feat(样式)`: 内置预设表 + 只写偏离 —— 程序内置 15 词完整样式表，文件里只写与默认不同的字段，如 `.样式{ 爱情: #E24B4A }`
- `feat(解析)`: 宽容通配 —— 全角半角逗号/顿号/冒号/分号、长短横线、`/* */` 注释块自动归一；引号内不归一（保护特殊名字）
- `feat(布局)`: 双颗粒度 —— `1: 甲,乙` 自动均分；`1(6): 甲,,乙` 声明槽位；`1[200px]:` 指定圈半径
- `refactor(底层)`: 重建为三层结构（界面无变化） —— 新增数据层与解析层，坐标不再存进数据、改由渲染时算出；为超点与多时间轴预留接入位置

## v0.14.20 · 2026-09-11（工程治理 + 公开镜像同步）

- `chore(发布)`: 工程治理与发布准备 —— 整理项目长期记忆文档（去冗余、留铁律）；同步 `web/` 公开镜像

## v0.14.19 · 2026-09-10（菜单与模式图标换新）

- `style(图标)`: 二级菜单与画板模式图标换新 —— 导入/保存七项加矢量图标；连线换环形连接、删线换橡皮擦、顶栏返回键换家图标

## v0.14.18 · 2026-09-10（图标语义修正 · 菜单精简 · 发布钮正负形）

- `fix(导入)`: 导入按钮图标方向反了（用成向下的下载箭头） —— 换成向上的上传箭头，方向才顺
- `refactor(菜单)`: 导入菜单精简为三项 —— 按「从零 → 增量 → 全量」排序：建立新连线图 / 增量导入数据 / 全量编辑数据
- `style(发布钮)`: 「发布小红书笔记」做正负形处理 —— 实心红底 + 反白文字，一眼看见这是主要动作
- `style(导出菜单)`: 调整顺序与措辞 —— 发布笔记 → 导出图片 → 导出完整数据 → 导出人物名单

## v0.14.17 · 2026-09-10（面板拖动跟手 + 强制优先级收敛）

- `feat(面板)`: 拖动真正跟手 —— 高度实时跟手 + rAF 合并写样式（高刷不掉帧）+ 8px 起手阈值 + 松手吸附前强制回流
- `refactor(熵减)`: 强制优先级声明收敛 —— 全站 `!important` 从 5 处收敛到 1 处（仅留全局工具类 `.hidden`）
- `fix(面板)`: 删掉压死拖动手感的那条声明 —— `#panel.dragging{ max-height:none !important }` 压掉了 JS 实时高度；改为只关过渡
- `test(门禁)`: 新增 Q1–Q6 断言（含 `!important` 熵减门禁）

## v0.14.16（三问号 CSS 级归一 + 项目文档归档整理）

- `style(帮助钮)`: 三个「?」改为共用同一条样式规则 —— 顶栏/面板/弹窗合并为一条，与「写法说明」钮同源
- `chore(归档)`: 旧文档与历史安装包归档 —— 根目录 22 份精简到 14 份，8 份已完结文档 + 6 个历史 zip 移入 `_rubbish/`（只归档不删）

## v0.14.15（三问号统一 + 图例描边归一 + 名字滑块开关）

- `feat(帮助钮)`: 三个「?」样式统一 —— 改用同一款描边问号图标，尺寸与「写法说明」钮一致
- `style(图标)`: 其他小图标统一 1.2rem —— 图例圆点、「添加轨道」+ 号
- `feat(人物页)`: 「显示名字」改成滑块开关 —— 与「槽位」同款，挪到「头像显示」行
- `style(图例)`: 两行描边统一用 border —— 本命圆点与爱情短横条视觉粗细一致
- `style(图标)`: 喜好度心形图标重画 —— 填充式、跟主题走，夜间自动变白
- `fix(弹窗)`: 标题行「?」与「×」上下居中 —— 修 svg 行内基线残留空隙

## v0.14.14 · 2026-09-10（夜间可读性第二轮）

- `fix(夜间)`: 按钮文字 / 图标全部变白（根因修复） —— 浏览器对 button 默认不上继承色，深底黑字看不见；补普通 `.btn` 颜色继承
- `style(布局)`: 圈行操作图标放大并靠右 —— 删除图标 14px → 20px，推到行最右
- `style(帮助钮)`: 「?」帮助钮去掉外框容器 —— 只留图标本体并放大到 28px（透明内边距保 40px 触点）
- `fix(样式)`: 修复 v0.14.13 一条样式被写坏 —— 误把两条规则挤成一行并混入坏字符，致人物列表按钮对齐失效

## v0.14.13 · 2026-09-10（控件尺寸统一 + 白底清理 + 箭头 chip 间隔）

- `style(控件)`: 图例圆点 / 帮助按钮 / 取色器尺寸统一对齐 —— 图例色点 1.4rem、帮助钮 2rem、取色器 1.5rem，按 rem 规格化
- `fix(连线)`: 箭头 chip 图标与文字之间加间隔 —— 给 icon 包 `<span class="chip-ic">` 让 `.chip` 的 gap 生效
- `fix(人物列表)`: 白底清理 + 操作图标对齐 —— 夜间 `<select>` 白底改跟面板；删 `.pm-star` 基线 hack
- `style(配色)`: 日间模式色块描边改浅灰蓝 `#c9d1d9`

## v0.14.12（表格化对齐：行高统一 + 组件复用）

- `style(面板)`: 行高统一 + 内容垂直居中（表格化） —— 新增行高令牌 `--row-h`，四类「一行」统一取同一高度
- `refactor(控件)`: 「日间 / 夜间」改用连线模式的胶囊段控件 —— 不再另造轮子，跨 Tab 视觉语言统一
- `fix(布局)`: 图标与文字「不在一条中线」的基线补丁清理 —— 删 `vertical-align` 硬凑，改 flex 交叉轴居中

## v0.14.11 · 2026-09-10（样式面板收口 + 色块描边实色）

- `style(样式面板)`: 背景色并进「外观」行 —— 配色相关开关集中一处，少一行
- `style(配色)`: 色块描边改实色（去掉透明度） —— `--legend-edge` 由半透明 `rgba()` 改不透明实色，昼夜都看得清
- `style(面板)`: 面板把手去掉上/下箭头 SVG —— 只留中间那根短杠，结构干净

## v0.14.10 · 2026-09-10（全局文案软代码化）

- `refactor(文案)`: 全局文案软代码化（i18n 字典） —— 长文档类文案集中到 `js/i18n.js`，改字不动 `ui.js`；为国际化预留入口

## v0.14.9 · 2026-09-10（示意图落地第二批：行内收纳）

- `style(面板)`: 行内收纳 —— 「批量编辑名单」圆钮进「头像显示」行右端；「头像显示」改胶囊段控件；三行文字标题删除、笔刷 chips 同行横滑；ⓘ 进排布行右端
- `style(缩放条)`: 只留百分比数字 —— 删「双指缩放 · 拖动空白平移」提示

## v0.14.8 · 2026-09-10（位置回调 + 开槽不转圈）

- `fix(状态徽章)`: 被误挪到右上 —— 回归左下角原位
- `style(缩放条)`: 从画布上方独立一行改为右上角悬浮胶囊
- `fix(布局)`: 开槽位不再整圈「转圈圈」 —— 锚点角色原地保持角度，其余相对等分

## v0.14.7 · 2026-09-10（圆心星标夜间描边）

- `fix(人物列表)`: 「设为圆心」空心星夜间隐身 —— 外链 `<img>` 描边硬编染不到，改内联 SVG + `currentColor` 跟昼夜

## v0.14.6 · 2026-09-10（连线 Tab 布局重排）

- `style(连线)`: 笔刷三栏改单行横向滑动 —— `nowrap + overflow-x:auto`，不再折行占三行
- `style(连线)`: 「批量编辑连线」「清空全部连线」收进「连线模式」胶囊同行右侧，改纯图标按钮

## v0.14.5 · 2026-09-10（抓手 chip 移右上 + 图标重排）

- `style(抓手)`: 模式指示 chip 移到右上（紧贴缩放指示下方）
- `style(图标)`: chip 内 emoji（👋🌐👤🎨）改 SVG（`hand`/`layout`/`person`/`style`），跟昼夜翻转
- `fix(图标)`: 「情感指向」前的小箭头语义不清 —— 换为朝右单箭头
- `style(人物)`: 「批量编辑名单」改圆形浮动按钮（`.circ-fab` 36×36）

## v0.14.4 · 2026-09-10（笔刷图标换化妆刷 + 冗余 hint 清扫）

- `style(图标)`: 「底层粗线笔刷」换化妆刷 —— 原铅笔 SVG 与「笔刷」语义不符
- `chore(文案)`: 7 处冗余提示清除（input.js 5 处 toast + 样式 Tab 2 处 hint）

## v0.14.3 · 2026-09-10（圆点外描边跟昼夜）

- `fix(图例)`: 夜间「不吃」色融底 —— 底层圆点加 1px 描边；新增 CSS 变量 `--legend-edge`，昼夜各一档

## v0.14.2 · 2026-09-09（提示文本夜间读糊）

- `fix(连线)`: 「暂无连线」段夜间读糊 —— 新增 `body.night .hint` 提升亮度

## v0.14.1 · 2026-09-09（UI 打磨第一轮）

- `style(布局)`: 「平均排布」「添加轨道」弃 emoji 改线条 SVG，描边转 `currentColor`
- `fix(弹窗)`: 批量编辑连线文本框夜间黑字叠深底看不清 —— 补 `color: var(--ink)`

## v0.14.0 · 2026-09-09（数据层重构第一批 + 容器布局修复）

> 🚧 未完成部分：一段式导出格式（行语法）由外部设计中；「上传新增数据 / 编辑已有数据」两页 UI 待格式定稿后落地。
> ⚠️ 真机验证待做：容器顶/底栏修复必须在小红书容器内多机型实测。

- `feat(数据)`: 喜好度唯一真源 = 角色自身涂色（`char.like` 单值） —— 同轴互斥、重复标记幂等
- `feat(数据)`: 圆心粗线就地折算 —— 任何入口画「圆心↔某人」粗线一律折算为角色涂色
- `feat(兼容)`: 旧数据静默迁移（`App.migrateCenterFav`） —— 打开旧草稿自动折算遗留圆心粗线
- `feat(命名)`: 角色名 / 图例名上限 20 字；图例名禁纯数字；新图例默认名递增
- `fix(容器)`: 画板在小红书里上下错位 —— 新增 `body.xhs` 容器态分支
- `fix(导出)`: 导出文本里同一角色重复出现 —— 喜好度行改由角色涂色生成，一人一次
- `fix(数据)`: 圆心建不出来的潜伏 bug —— `ring || 1` 把合法圈号 0 吞成 1
- `chore(脚本)`: 冒烟脚本中文目录解码修正（`fileURLToPath`）
- `test(门禁)`: 新增 `.workbuddy/smoke-v014-datalayer.mjs`（33 断言）

## v0.13.0 · 2026-09-09（信息同步重构）

> 🚧 待办：**箭头调整**（交接指定做进本版，不在已过审的 v0.12.0 上改）。
> ⚠️ 小红书容器版 **v0.12.0 已过审上线**，线上有 UI BUG 待修。

- `feat(顶栏)`: 标题可点改名（限宽省略号不挡按钮）
- `feat(关于页)`: 更新日志折叠 —— 只展开最近两个版本
- `feat(连线)`: 真箭头 —— 箭头处断开留白，几何集中 `App.ARROW` 常量
- `feat(样式)`: 预制色板横滑 + 圆点白黑双层描边；图例名输入框定宽；CSS 立 rem 基准
- `fix(面板)`: 挤压错位 —— 删常驻说明文字
- `fix(画布)`: 左下状态条冗余 —— 只显示非默认状态
- `chore(脚本)`: 冒烟脚本硬编码路径改自动定位
- `feat(编辑器)`: 人物名单 / 连线 / 完整数据 三 Tab 合一
- `feat(数据)`: 完整数据压缩导入导出（XHS2:），旧 JSON 快照仍兼容
- `feat(导入)`: 导入确认对话框（覆盖前提示，失败单独报错）
- `feat(菜单)`: 改名 —— 导出/导入完整数据
- `feat(连线)`: 编辑连线 modal 新增喜好度 / 关系 chips
- `feat(导出)`: 导出图片结果屏（关闭 / 新建 / 发布 / 存相册）
- `feat(角色)`: 角色批量上色模式
- `feat(兼容)`: 旧 v0.x 草稿自动兼容

## v0.12.0 · 2026-09-08（网页版）

- `feat(菜单)`: 「+」号径向绽开 —— 撤销/重做/导入/保存导出/关于 一圈按钮绕 + 弹出
- `feat(导入导出)`: 收进二级小窗
- `feat(顶栏)`: 帮助按钮「?」/「←」一槽两用，避开容器左上角

## v0.11.0 · 2026-09-08

- `feat(布局)`: 槽位布局真正可用 —— 每圈按槽数等分（默认保底 6 槽），空槽虚线占位
- `feat(布局)`: 槽位关 = 自由摆放（同一圈可有疏有密）
- `feat(人物)`: 批量编辑名单 —— 一个输入框同时当编辑/导入/导出
- `feat(连线)`: 连线记录点行即编辑（改箭头/翻转/删除）
- `feat(角色)`: 操作条改纯图标（星标 / 垃圾桶）
- `feat(视图)`: 定位坐标 ¤ 一键归位
- `feat(面板)`: 面板三态（收起 / 半开 / 全开）
- `feat(角色)`: 头像圆圈大小滑块（12–40）
- `feat(图例)`: 三表「显示 / 隐藏」👁
- `fix(夜间)`: 夜间模式不再把画板与导出图染黑
- `fix(导出)`: 手机（网页版）导出不再假下载，改全屏预览

## v0.10.0 · 2026-09-07

- `feat(容器)`: 适配小红书容器 —— 顶栏按钮移入画布悬浮工具条
- `feat(菜单)`: 菜单改底部弹出；面板可折叠
- `feat(图例)`: 喜好粗线 / 关系细线 / 箭头含义 分行展示
- `feat(连线)`: A→B 与 B→A 单箭头可共存，可分别配不同关系色
- `feat(连线)`: 箭头含义可自定义（默认「情感指向」）
- `feat(导入)`: 导入后自动缩放居中，圆心居中
- `feat(顶栏)`: 显示「填表人 的 图名」；圆心星标实心 / 空心两态

## v0.9.0 · 上线新版

- `feat(发布)`: 一键发布笔记
- `feat(导出)`: 导出图片（标题 / 署名 / 图例 一图成画）
- `feat(数据)`: 名单 / 快照导入导出，自动存本地草稿
- `feat(图例)`: 点图例色块直切对应笔刷
- `feat(连线)`: 连线规则对齐（同对后画覆盖、箭头方向可设）
- `feat(布局)`: 平均排布、增删轨道、拖拽换圈
- `feat(帮助)`: 全新帮助页与「关于」

## 🔮 未来前瞻（规划中，未实现）

- `feat(视图)`: 视图记忆 —— 导出或切 Tab 后保留当前缩放与平移位置
- `refactor(导入)`: 拆分「全量覆盖」与「增量导入」两个入口，全量覆盖提交后直接看到删除结果
- `feat(布局)`: 超点（一个角色占多圈/多槽）与多时间轴（Z 轴）可视化
- `refactor(UI)`: 二级窗口重排、一般 UI / 视觉重做、icon 替换、布局重构、手感升级
- `build(流程)`: 更新日志统一走约定式提交规范，`CHANGELOG` / 关于页 / `logbrief` 三处同源
