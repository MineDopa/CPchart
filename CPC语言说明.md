#### CP Chart 数据格式 · 语义定义 v2（CPC 语言）

> 本文档是 CPC 语言的**公开语义规范**，给人阅读、给 AI 当 prompt 直接投喂都可用。  
> 它定义了 CP Chart 导出 / 导入所使用的纯文本数据格式：如何写角色、如何排圈、如何表达关系与方向、自建关系词。  
> 代码基线：v0.14.x（数据层重构已落地）。配套 AI 生成指引见文末 §11。

---

## 0. 这份文档要过什么（北极星，验收三标准）

CPC 语言是否合格，只认这三条：

1. **AI 可生成**：给任意 AI 一段 `prompt + 作品名`，它能吐出**完美符合 CPC 语义**的角色关系文本，不用再教格式——包括**自主决定布局**（谁放圆心、谁放第几圈、圈内怎么排序，见 §11.1）。
2. **导入无歧义**：任何一段合法 CPC 文本，程序**只能有一种**合法解读，且能正常导入、往返一致（导出→导入→再导出逐字节相同）。
3. **AI 可读图**：给 AI 一张 CP Chart 风格的同心圆关系图 + 本文档，它仅靠看图就能反推出等价的 CPC 文本（含圈号与顺时针顺序，见 §11.2）。**图 ⇄ CPC ⇄ 状态数据库，三方互转。**

> 一切语法取舍，以同时满足这三条为最高优先级。

---

## 1. 定位与设计哲学（先读，决定了后面所有规则）

### 1.1 三条铁律

1. **给"会犯错的小白"写，不是给"严格执行的 AI"写。** 解析器必须容错通配，不能指望人像机器一样准确。→ 见 §5 宽容输入。
2. **极简录入 vs 精细定制分流。** 大多数导入走默认样式（文本只写最小信息：位置/类型/方向）；少数想精调才写 `#样式#`。→ 见 §1.2。
3. **纯文本、可手写、可复制粘贴。** 容器内无文件下载，数据靠复制/粘贴流转；文本必须能打、能贴进评论区不被吞。→ 不带图、无 base64、无 URL。

### 1.2 数据 vs 视觉分离（CSS 思维）

一条连线在数据上是**一个结构**，在视觉上是**一堆可独立调整的样式旋钮**：

- **数据** = 这条线连谁、属于什么类型词、什么方向。
- **视觉** = 颜色 / 粗细 / Z 轴 / 箭头 / 形状 —— **彼此正交、可分别覆盖**。

**关键**：粗细**不是**"底层"的本质，只是底层线的**默认样式**。当前版本保留顶层/底层两种数据结构，粗细走各自默认；"单条调粗/调细"旋钮**推迟到未来版**，本轮**不**为它在格式里占位。

> ⚠️ 术语澄清（易混点，已定案勿再混淆）：
> - **底层（bottom）/ 顶层（top）** = 两类**类型词**的分组：底层词表达示意**强度**（默认粗线），顶层词表达示意**定义**（默认细线）→ 见 §2。
> - **`top` / `bottom` 在 `#样式#` 里** = **Z 轴叠放层级**参数，只决定"线重叠时谁压谁上面"，与粗细无关。粗细永远由 `px` 值决定。
> - 两处"top/bottom"字面相同、含义不同：前者是**类型词分组**（数据），后者是**Z 轴参数**（样式）。

### 1.3 双通道（为什么 CPC 不带图）

- **CPC 纯文本**（本文）：给人看 / 改 / AI 读 / 贴评论区 / 批量导入。**只承载数据语义，不带图。**
- **压缩字符串**（web 全量，已落地 v1.0 LZString + `XHS2:` 前缀）：机器级完整备份，**头像等图片走这条通道**。

角色数据模型需**预留"头像可被外部 API 填充"接入点**（Bangumi API，Web 版专属）。

### 1.4 文件名后缀约定

CPC 本质是**纯文本**，不依赖任何特定后缀——只在导入导出时用一下，保存成 `.txt`、`.cpc` 或任何名字都可以，工具导入时认内容不认文件名。

- 纯文本、不带图、无 base64、无 URL（见 §1.1 铁律），用记事本 / 任意文本编辑器打开即可读可改。
- 若想要一个专属后缀便于识别，可用 `.cpc`（CP Chart 缩写），但这只是习惯，不是规定。
- 程序不靠后缀名判断格式：粘贴进「连线」Tab 的文本，解析器按内容（五段 `#段名#` 结构）识别，后缀只是给人看的约定。

---

## 2. 数据模型真相（CPC 必须表达的东西）

程序维护一个状态数据库（`App.state`）。核心模型：**两个正交维度叠在同一条连线上 + 一条独立的单角色轴。**

### 2.1 连线的两个正交维度（★核心模型）

一条 A-B 连线可同时承载两个互不依赖的维度——**"我多喜欢" × "这是什么"**：

| 维度 | 词表（默认） | 回答的问题 | 默认视觉 | 唯一键 |
|---|---|---|---|---|
| **强度档**（底层 bottom） | 本命 · 很喜欢 · 路好 · 不吃 | **我多喜欢这段关系** | 粗线 | **无向对** `{A,B}`（同对一条，后写覆盖） |
| **定义档**（顶层 top） | 爱情 · 友情 · 亲情 · QPR…（可自建） | **这段关系是什么** | 细线 | **有向对** `A→B` 与 `B→A`（两条可并存，各配不同定义） |

六个典型表达全部成立（强度 × 定义自由组合）：

| 表达 | 强度档 | 定义档 |
|---|---|---|
| (a) 我很喜欢他俩的爱情 | 很喜欢 | 爱情 |
| (d) 官配是爱情，但没那么上头 | （低档或不标） | 爱情 |
| (e) 很磕，但它其实是友情/QPR | 很喜欢 | 友情 / QPR |
| (f) 很磕整对，且 A→B 爱情、B→A 友情 | 很喜欢（整对） | 爱情 A→B + 友情 B→A（两条） |

**方向规则（已定案）**：
- **方向对两层都开放**。`很喜欢：A->B` = **一条带方向的粗线**（不是拆成两条）——方向表达"攻受/谁指向谁"的偏好。
- 视觉渲染时箭头默认画在细线上（粗线叠在下面）；但**存储**上 bottom 记录同样有 arrow 字段。渲染规则 ≠ 存储规则。
- 无向 = 只表达"有连"，方向 = `A→B` 单向 / `A<->B` 双向。

**两个维度可单独存在**：
- 只画强度（懒人路径）：`很喜欢：A-B` —— "我喜欢这对，定义/攻受无所谓"
- 只画定义：`爱情：A-B` —— "这是爱情，强度未标"
- 叠加：两行都写。

### 2.2 单角色轴（独立于连线）

- **单角色喜好度**：`char.like`，**单值**（一角色只能一档）→ 视觉 = 角色圆被涂色。与连线无关、与圆心无关（v0.14 起，画到圆心的粗线会被程序**就地折算**成该角色涂色）。

### 2.3 图例（自建 = 默认，无差别）

每个类型词本质只是一条**数据行**：`{ 词, 颜色 }`（+未来版 px/Z轴）。默认词与新词**无本质区别**，导入新增时同样建一行。→ 见 §4.6。

### 2.4 哪些属于"看的人偏好"，不进 CPC

`ui` 里的夜间模式、字号、`slotMode` 开关、线宽 `thinW` 等 —— 是**看的人**的设置，不是图的数据。

---

## 3. 完整文件结构（骨架）

一段 CPC 文本 = 一个**注释头** + 若干个**分段**。分段用 `#段名#` 声明，行尾用 `;` 或换行结束，条目间用通配分隔（§5）。

> ✅ 下方骨架样例**角色自洽**（`#连线#` / `#喜好#` 的人名都在 `#布局#` 里）：这是 CPC 的**第一硬性要求**，详见 §11.1「角色自洽铁律」。

```
/* 该数据由 CP Chart 导出 | 解读格式见「CPC 语言说明」 */
#标题# 红楼梦 角色关系图
#布局#
1: 贾宝玉,林黛玉,薛宝钗,史湘云;
2(10): 贾母,王夫人,贾政,王熙凤,贾琏,袭人,晴雯,紫鹃,薛姨妈,尤二姐;
#喜好#
本命：贾宝玉,林黛玉; 很喜欢：薛宝钗;
#连线#
爱情：贾宝玉<->林黛玉, 贾宝玉->薛宝钗;
友情：贾宝玉-史湘云, 林黛玉-紫鹃;
敌对：王熙凤->尤二姐;
#样式#
敌对：#7B2FBE, 2px, top;  爱情：#E24B4A, 3px, bottom;
```

| 分段          | 必填    | 职责                      |
| ----------- | ----- | ----------------------- |
| 注释头 `/* */` | 否     | 来源信息，解析忽略               |
| `#标题#`      | 否     | 图名（≤18字）                |
| `#布局#`      | 视导入语义 | 谁在哪圈、顺序、槽位、可选半径         |
| `#喜好#`      | 否     | 单角色涂色（`char.like`）      |
| `#连线#`      | 否     | 关系：起终点 + 方向 + 类型        |
| `#样式#`      | 否     | 图例定义/覆盖：颜色+粗细+Z轴（可自建新词） |

> 段的出现顺序**建议固定**如上，但解析器不应强依赖顺序（宽容）。同段可拆多行。

---

## 4. 各段语义细则

### 4.1 注释头

`/* ... */` 整块忽略。允许多行。

### 4.2 `#布局#` —— 顺序是数据，双颗粒度解析

**一行 = `圈号(槽位?)[半径px?]: 角色列表;`**

| 写法                     | 含义                                                 |
| ---------------------- | -------------------------------------------------- |
| `1: A,B,C`             | 第 1 圈放 A/B/C，槽位**均分**（程序自动等角）                      |
| `1(8): A,B,C`          | 第 1 圈共 **8 槽**；列了 3 人 → 空槽默认排末尾（A/B/C 占 1-3，4-8 空） |
| `1(8): A, ,B, , , ,C,` | **显式空槽占位**（`空元素`=空坑）：A占1, 空2, B占3, 空456, C占7, 空8   |
| `1[150px]: A,B,C`      | 该圈**半径 150px**（不写走默认等距规则）                          |

**顺序语义**：列表顺序 = 从**起点（正上方，12点方向）**&#x8D77;，按列出顺序**顺时针**排一整圈。A 挨着 B、B 挨着 C —— **顺序本身是数据，不能丢**。不带槽位时，有几人就等分几个坑。

**圈号**：`0` 或 `圆心` = 最中间（单角色）。

**容错**：带 `(槽位)` 却**超员**（列的人数 > 槽位数）→ 报错 + 给"自动改正/返回"按钮，**不静默扩容**。

> ⭐ **双粒度定位**：`#布局#` 的槽位/半径是**可选参数**，谁写谁不写都合法，导入器都零报错地接纳：
> - **人类手输（最简粒度）**：可只写 `圈号: 名单;`——不写 `(槽位)` 程序按该圈人数**自动均分**；不写 `[半径px]` 走程序默认等距（第1圈 150，之后逐圈 +120：150/270/390…）。适合在导入框快速手敲。
> - **AI 生成（详尽粒度）**：**必须**每圈写 `(槽位数)`（≥ 该圈人数），并给每圈写 `[半径px]`（由内向外递增，可自由覆盖默认）。AI 输出写得越详尽，越能压测导入器的槽位/半径解析——这是**特性**，不是缺陷。
> - 两种粒度**不必二选一**：一份文件里有的圈写全、有的圈省略，也合法（逐圈独立）。
> - 本定位服务于北极星三标准里的「AI 可生成」与「导入无歧义」：给 AI 的 Prompt（`CPC-AI导出Prompt.md`）走详尽口径，给人看的极简手输走最简口径，二者由同一套导入器无差别解析。

### 4.3 `#喜好#` —— 单角色涂色

**一行 = `喜好词：角色A,角色B,角色C;`**

- 读作：这些角色各自的 `char.like` 都设为该词。
- **同角色写多次 = 后写覆盖**（同轴单值），不是追加。
- 一个角色只会出现在一条喜好行（同轴互斥）。

### 4.4 `#连线#` —— 类型 + 方向（图例前置式）

**一行 = `类型词：A 方向符 B, C 方向符 D;`**

方向符（§5 通配后，半角/全角箭头族都认）：

| 语义     | 标准写法    | 全角等价（都可） | 说明                   |
| ------ | ------- | ------- | -------------------- |
| 无向     | `A-B`   | `A—B` `A－B` `A–B` | 横线通配（长/短/减号都可），只管"连" |
| 单向 A→B | `A->B`  | `A→B` `A➔B` `A⟶B` | `>`/箭头 表方向          |
| 反向 B→A | `A<-B`  | `A←B` `A⇐B`    | 反向箭头（导入后存为 A→B）     |
| 双向     | `A<->B` | `A↔B` `A⇄B` `A⇆B`    | 两头都指              |

**方向对强度词、定义词都开放**（§2.1 已定案）：

```
爱情：A->B；       ← 定义档：A→B 爱情（细线）
友情：B->A；       ← 定义档：B→A 友情（与上行可并存——有向对是两条记录）
很喜欢：A->B；    ← 强度档：这条粗线带方向（一条有向粗线，表达"我磕 A→B"）
本命：A-B；      ← 强度档：无向，"这对是我本命"
```

- **同一对可出现在多行、各带一个类型词**（它们是叠加，渲染合并成一条视觉线，粗线在下细线在上）。
- 强度词（底层）+ 定义词（顶层）可任意组合：`很喜欢：A-B` + `爱情：A->B` = "我很喜欢他俩，且 A→B 是爱情"。
- **有向对 A→B 与 B→A 是不同记录，不合并**；无向粗线同对只有一条，后写覆盖。

**类型词未知时**：自动新建为**顶层定义词**（颜色取调色板下一个）——强度档几乎只有默认四词，用户新建词绝大多数是关系定义（敌对/主从/师生…）。想精确控制颜色或建底层强度词，写进 `#样式#`（z 参数决定层）。

### 4.5 角色名转义（引号包裹）

角色名含**语法字符**（`，,、：: - — > <` 等）时，用引号包住整个名字：

```
#布局#
1: 'Emet-Selch', 让-雅克·卢梭, Azem;    ← 'Emet-Selch' 带引号（含连字符其实安全，示范写法）
#连线#
爱情：'Emet-Selch'->Azem;
```

- 引号**可选**：正常名字（不含语法字符）不用加。中文人名直接写。
- 解析器遇到 `'...'` / `'...'` 时，引号内整体算一个名字，不做语法切分。
- **漏写引号导致解析歧义时**：报错 + 提示"名字含特殊字符，请用引号包起来"，不静默猜。
- 导出端：名字含语法字符时**自动加引号**，人无需操心。

### 4.6 `#样式#` —— 图例定义 / 覆盖（可自建新词）

**一行 = `类型词：颜色, 粗细px, Z轴;`**

```
敌对：#7B2FBE, 2px, top;     ← 覆盖/新建"敌对"图例
爱情：#E24B4A, 3px, bottom;  ← 覆盖"爱情"
```

- 三要素：**颜色(hex) + 粗细(px) + Z轴(top/bottom)** —— 画一条线必须的 3 个要素；不写则用默认。
- **自建新词与默认词无差别**：`敌对` 若默认表没有，这行就是**新建**；有则是**覆盖**。程序自动把它当一条图例数据行处理。
- Z 轴 `top/bottom` **只控制叠放**，与粗细无关。
- `#样式#` 只在"想给某图例换色/换粗细/换层级，或新增一个词"时出现；纯导入默认图例不必写。
- **内置样式预注入（已定案）**：默认词（本命/很喜欢/路好/不吃/爱情/友情/亲情/QPR）在没有 `#样式#` 声明时**不是未定义样式**，而是程序内置的一套预设（颜色 + 默认粗细 + 默认层级）。用户写 `敌对：` 而不写 `#样式#`，`敌对` 走自动配色；写了 `#样式#` 则按声明覆盖。→ 导出的图例**永远有完整可用样式**，导入方无需猜。

---

## 5. 宽容输入（小白容错通配）

**解析前先做"归一化"**——把以下变体都归成同一种含义，避免人类口误导致报错：

| 归类     | 接受的写法 → 归一成               | 说明               |
| ------ | ------------------------- | ---------------- |
| 名单分隔符  | `，` `,` `、` `;` `；` 空格 换行 | 都 = "下一个名字/条目"   |
| 冒号     | `：` `:`                   | 段名与条目分隔          |
| 无向连线横线 | `—` `-` `－` `–`           | 都 = "无向连接"（横线通配） |
| 单向箭头（→） | `->` `→` `➔` `➜` `➞` `⇒` `⟶` | 都 = "A 指向 B"      |
| 反向箭头（←） | `<-` `←` `⇐` `⟵`           | 都 = "B 指向 A"（存为 A→B） |
| 注释     | `/* */` 各种换行残留            | 忽略               |
| 空白     | 行首尾、条目间多余空格               | trim             |

**关键规则（消除歧义）**：方向**完全由 `>` / `<` 决定**。横线本身不决定方向——解析时**先找 `>`/`<`**，有则按方向读；从头到尾无 `>`/`<`，则中间横线（不论长短）全作无向。

**容错 ≠ 歧义**：上面这些"不同拼法"最终**都落到同一种解读**；绝不出现"同一串字有 2 种读法"。这同时满足"宽容"与委托书"歧义清零"。

**符号归一顺序**（避免撞车）：先拆 `方向符`（`<->` `->`），再拆 `无向横线`，最后才是 `分隔符`。

---

## 6. 排序规则（导出稳定，保证往返一致）

委托书已定 + 补充：

- **`#连线#`**：按箭头**起始人物名字字典序**（非创建顺序）。有向对 A→B 排在 B→A 前时看首名。
- **`#布局#`**：按圈号从小到大（0 → 1 → 2…），圈内按**角色角度顺序**（即顺时针列表序）。
- **`#喜好#`**：按喜好词分组，组内角色名字典序。
- **同名字冲突**次级键：按 `id` 稳定序（程序内部 id），保证逐字节往返。

---

## 7. 边界与容错规则汇总

| 情况           | 行为                          |
| ------------ | --------------------------- |
| 空文本          | 视为空导入，无操作                   |
| 只有圆心         | 正常；`#布局#` 可 `0: A`          |
| 无圆心角色        | 正常；喜好度仍作用于 `char.like` 涂色   |
| 20 字满名       | 截断（`clipName`），导入时提示        |
| 名含空格/标点      | 引号包裹转义（§4.5）；漏写引号产生歧义时报错引导，不静默猜 |
| A→B 与 B→A 并存 | 两独立记录，均保留                   |
| 同对同时有粗线细线    | 拆多行表达，渲染合并（§4.4）            |
| 图例被隐藏        | `#样式#` 不表达 show 状态（见 §9 局限） |
| 重复行 / 重复导入   | 幂等：导入两次状态不变                 |
| 纯数字名         | 图例名禁纯数字；角色名按现有判定规则区分圈号      |
| 类型词未知        | 自动建图例（或经 `#样式#` 显式定义）       |

---

## 8. 往返一致性与增量/覆盖语义

- **往返幂等**：导出 → 导入 → 再导出，文本逐字节相同。破坏点主要在两处：① 图例 `show` 隐藏状态（见局限）；② 手工微调过的非均分角度（见局限）。
- **增量导入**（上传新增）：只增不删，已有保持，幂等。
- **覆盖导入**（编辑已有 / 全量替换）：删掉某行 → 图上对应消失；需二次确认（REPLACE）。

---

## 9. 局限与未决（诚实声明）

1. **图例 `show` 隐藏状态**：`#样式#` 暂不表达"某词被隐藏"，导入后隐藏项会恢复显示。后续可加 `hide` 关键字。
2. **手工微调角度**：纯文本只能表达"相对顺序 + 槽位"，无法还原用户**手动拖拽**产生的非均分角度。精确还原需走压缩串通道。
3. **头像/图片**：CPC 不带图（双通道架构，见 §1.3）；头像由压缩串或 Bangumi API 承载。
4. **背景色**：不进格式；每次新建走默认。
5. **粗细解绑旋钮**：未来版再加"单条调粗/细"，本轮格式不占位（但 `#样式#` 的 px 已留扩展口）。当前 `#样式#` 里 px/Z轴 参数**解析时接受但暂不生效**（数据模型只有 color 字段），颜色完全生效。
6. 本文基于 **v0.14.x 代码核对** + 工具作者口述定稿；落地后已通过冒烟测试。

---

## 10. 落地范围（给开发 / 对齐用）

定稿后只需改 **`js/io.js`** 的 3 个函数（委托书 §5 已列）+ `#布局#`/`#样式#` 解析新增，其它模块不受影响。详细逐处改动清单 = 委托任务书 §5 + 本文 §4-§7。

**落地执行顺序建议**（按风险从低到高）：

1. 归一化层（§5 通配）→ 独立函数，先做，收益最大
2. `#布局#` 解析（顺序→角度/槽位）
3. `#连线#` 改 `类型：A->B` 图例前置式（替换旧 `A—类型—B`）
4. `#喜好#`（接现有 `char.like` 逻辑）
5. `#样式#`（图例定义/覆盖/自建）
6. 往返一致性 smoke 脚本

每步过一遍 §0 三标准（AI 能否生成 / 导入有无歧义 / AI 能否读图）。

---

## 11. AI 转换指引（给 AI 的操作手册）

> 本节写给执行转换的 AI：拿到这份文档后，你可以在三种方向上做转换——
> **A. 构思/资料 → CPC**（生成）、**B. CPC → 图**（程序导入，你只管生成合法文本）、**C. 图片 → CPC**（读图转录）。

### 11.1 生成指引：构思 → CPC（AI 当"构图者"）

给 AI 一段 `prompt + 作品名`（或一份角色关系描述），AI 要**自主完成两件事**：决定布局、写出 CPC。

> 🎯 **实操建议**：别只发本文件给 AI 让它"看着办"——请直接使用配套的《CPC-AI 导出 Prompt》（根目录，可整份复制），它把下面这些约束打包成了 AI 会执行的强指令，实测比单发本规则书生成质量高得多（单发规则书时 AI 常犯"连线用未定义角色"）。

**★角色自洽铁律（生成质量的头号命门）**：

1. `#布局#` 段是**角色清单的唯一来源**。任何人想上图，必须先写进 `#布局#`。
2. `#连线#` / `#喜好#` 出现的**每一个人名，必须已在 `#布局#` 一字不差地定义过**。程序在 `#连线#` 找不到该角色会报「未知名」并丢弃这条——最常见的 AI 错误是写关系时"临时想起"某人直接写进连线却漏了布局。
3. 输出前必须做**人名守恒自检**：连线+喜好的人名 ⊆ 布局人名。

**布局自主决策原则**（谁放哪，AI 自己定）：

1. **圆心（`0:`）** = 全图视角的核心人物。通常是主角 / 提问者 / "我"。只有一个圆心。
2. **圈的亲疏语义**：第 1 圈 = 与核心关系最紧密的角色；越外圈越边缘。判断依据 = 该角色与圆心的**连线数量与强度**（本命/很喜欢 → 内圈；只有一条普通关系 → 外圈）。
3. **圈内排序**：让关系相近的角色**相邻**（顺时针），使连线短、少交叉。例如把同一家族的人排在一起、把与某人有多条连线的角色放其旁边。
4. **槽位数**：角色多、想留空位后续加人 → 写 `圈号(槽位数)`；不确定就省略，程序自动均分。**槽位数 ≥ 该圈人数**，超员会被报错。
5. 角色数 > 30 时建议分多圈且外圈槽位留白，避免拥挤。

**生成步骤**：

1. 从构思里抽取：角色清单、每对角色的强度（喜欢程度）、定义（关系类型）、方向（谁指向谁）。
2. 按布局原则排圈与顺序。
3. 按本文 §3-§4 语法输出五段。强度词/定义词**只用默认词表**（本命/很喜欢/路好/不吃 + 爱情/友情/亲情/QPR），确实需要新关系类型（如"敌对""主从"）才自建，并同步写 `#样式#` 给颜色。
4. 自检（★输出前必做）：**人名守恒**——`#连线#` + `#喜好#` 的所有人名 ⊆ `#布局#` 人名，缺一即补或删；强度词与定义词不混用（喜欢程度用前者、关系类型用后者）。

### 11.2 读图指引：图片 → CPC（AI 当"转录者"）

CP Chart 的图 = **同心圆轨道 + 放在轨道上的角色圆点 + 圆点间的连线**。按以下算法读图：

**第一步 · 定圆心**：图正中央的单一角色圆点 = 圆心，写 `0: 该角色名`。

**第二步 · 半径聚类成圈**：从圆心向外，按**到圆心的距离**把其余角色分成若干组——距离近似相等的一组 = 第 n 圈（n 从 1 起、由内向外递增）。AI 不必量出精确像素，**按视觉距离分组**即可。

**第三步 · 圈内排序（★圆形轨道的读法）**：对每一圈，以**正上方 12 点方向**为起点，**顺时针**依次读出该圈上的角色，按此顺序写入列表。顺序本身是数据（决定导入后谁挨着谁），不可乱。

**第四步 · 读连线**：逐条读角色圆点之间的连线：

| 图上看到 | 写成 |
|---|---|
| **粗线**（默认红/橙/黄/黑系、明显更粗） | 强度词行：`本命：A-B；`（词按颜色对照图例） |
| **细线**（默认粉/绿/蓝/紫系） | 定义词行：`爱情：A-B；`（词按颜色对照图例） |
| 线上**单侧箭头** | `-` 换成 `->`（箭头指谁，谁就是 B） |
| 线上**双侧箭头** | `<->` |
| **角色圆圈本身被涂色** | `#喜好#` 行：`词：该角色;`（涂色 = 单角色喜好度，不是连线） |

**第五步 · 对照图例**：若图上带图例行（底部"本命/很喜欢/…"带色样），按色样映射词名；图上没有图例时按 §2.1 默认词表的默认配色反查（本命红 #d32f2f、很喜欢橙 #f57c00、路好黄 #fbc02d、不吃黑、爱情粉 #ec407a、友情绿 #43a047、亲情蓝 #1976d2、QPR 紫 #8e24aa）。

**第六步 · 输出自检**：`#布局#` 人数 = 图上圆点数；每个连线的人名都能在 `#布局#` 找到；一对角色之间多条线（粗+细）拆成多行各写一个类型词。

> 难点提示（为什么需要本文档）：**圆形轨道上"谁在第几圈、顺时针次序"无法靠横竖坐标直觉判断**，必须走"半径聚类 + 12点起顺时针"算法——这正是 AI 裸读图容易出错的地方。

### 11.3 互转闭环示例

```
图片（同心圆+连线） --§11.2 读图算法--> CPC 文本 --程序导入--> 状态数据库
状态数据库 --程序导出--> CPC 文本 --§11.1 反向检查--> 图片（AI 依文本重建构图）
```

AI 拿到 CPC 文本想"想象这张图"时：`#布局#` 给圈层与顺时针次序，`#喜好#` 给圆点颜色，`#连线#`+`#样式#` 给线（颜色/类型/方向）；底层词=粗线在下、顶层词=细线在上，同对多行合并成一条视觉线。

---

## 附：完整样例（可直接喂给 AI 验证）

> ✅ 本样例**角色自洽**：`#连线#` / `#喜好#` 出现的每一个人名，都在 `#布局#` 出现过（共 24 人，一字不差）。AI 生成时必须同样自洽（铁律见 §11.1 自检 3 与《CPC-AI 导出 Prompt》）。

```
/* 该数据由 CP Chart 导出 | 解读格式见「CPC 语言说明」 */
#标题# 红楼梦 角色关系图
#布局#
1: 贾宝玉,林黛玉,薛宝钗;
2(12): 贾母,王夫人,贾政,王熙凤,贾琏,袭人,晴雯,紫鹃,薛姨妈,贾探春,薛宝琴,平儿;
3(12)[350px]: 贾元春,贾迎春,贾惜春,李纨,妙玉,史湘云,秦可卿,贾巧姐,赵姨娘,贾环,尤二姐,贾芸;
#喜好#
本命：贾宝玉,林黛玉; 很喜欢：薛宝钗,史湘云;
#连线#
爱情：贾宝玉<->林黛玉, 贾宝玉->薛宝钗;
亲情：贾母-贾宝玉, 贾政-贾宝玉, 王夫人-贾宝玉;
友情：贾宝玉-史湘云, 林黛玉-紫鹃, 薛宝钗-史湘云;
敌对：赵姨娘->贾宝玉, 王熙凤->尤二姐, 贾环->贾宝玉;
主从：贾宝玉-袭人, 贾宝玉-晴雯, 林黛玉-紫鹃;
#样式#
敌对：#7B2FBE, 2px, top;
```
