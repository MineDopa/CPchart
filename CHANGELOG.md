# 版本更新说明 · CP Chart（网页版）

> 网页版与工具内「关于」页更新日志同源。以下为公开发布版，按大版本归档。

## v0.15.0 · CPC 数据语言落地
- ➕ **CPC 分段文本格式（导出 / 导入全链路）**：数据可导出为纯文本 CPC（`#标题#` / `#布局#` / `#喜好#` / `#连线#` / `#样式#` 五段），人和 AI 都能直接读懂、也能批量生成；文本可直接复制粘贴、贴评论区或投喂给 AI。
- ➕ **宽容通配解析**：全角半角逗号 / 顿号 / 冒号 / 分号、长短横线、`/* */` 注释块自动归一；名字含特殊符号用引号包裹即可。
- ➕ **布局双颗粒度（顺序 = 数据）**：`1: 甲,乙` 自动均分；`1(6): 甲,,乙` 声明槽位与空槽；`1[200px]:` 指定圈半径；圈上按书写顺序从 12 点方向顺时针摆放。
- ➕ **样式段**：一行自定义颜色或新增关系类型词，`z` 参数决定建到哪一层；默认词内置预设样式，不写也有完整配色。
- ➕ 完整语法与 AI 转换指引见同目录 **`CPC语言说明.md`**。

## v0.14.x · 数据层重构 + UI 打磨
- ➕ 喜好度唯一真源改为「角色自身涂色」（`char.like` 单值），不再靠圆心粗线表达。
- ➕ 圆心粗线就地折算为角色涂色；旧草稿自动兼容迁移。
- ➕ 角色名 / 图例名上限 20 字，图例名禁纯数字。
- ➕ 多轮 UI 打磨：图标改 SVG 跟昼夜、提示文本夜间可读性、笔刷与模式图标化、连线 Tab 横滑布局等。

## v0.13.0 · 信息同步重构
- ➕ 统一编辑器：人物名单 / 连线 / 完整数据 三 Tab 合一。
- ➕ 完整数据压缩导入导出（XHS2: 格式），旧 JSON 快照仍兼容。
- ➕ 导入确认对话框、编辑连线弹窗、角色批量上色模式。
- ➕ 旧 v0.x 草稿自动兼容。

## v0.12.0 · 移动端交互
- ➕ 菜单改为「+」号径向绽开，单手好点。
- ➕ 导入 / 保存导出收进二级小窗。
- ➕ 顶栏帮助按钮随时打开操作指南。

## v0.11.0 · 槽位布局
- ➕ 开槽位后每圈按槽数等分铺位，空槽虚线占位。
- ➕ 槽位关 = 自由摆放，拖动可停在轨道任意角度。
- ➕ 批量编辑名单、连线点行即编辑、三表显隐、夜间模式只改外壳。

## 🔮 未来前瞻（规划中，未实现）
- 视图记忆：导出或切 Tab 后保留当前缩放与平移位置。
- 二级窗口重排、一般 UI / 视觉重做、手感升级。

---

许可证：MIT
