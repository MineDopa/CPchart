# CPC-AI 导出 Prompt（可直接整体复制发给 AI）

> 来源：CP Chart 项目
> 日期：2026-09-11（块语法版）
> 用途：把这段**整份**贴给任意外部 AI（ChatGPT / Claude / 豆包 / 通义…），末尾的 `【替换】` 填作品名，AI 就能产出一份**导入零报错、角色自洽、布局详尽**的 CPC 角色关系文件。
> 配套文档：同目录 `CPC填写说明.md`（完整填写说明）。
> ⚠️ **本版换代**：CPC 已从「分段制」（`#标题#` 一行行写）换成「块语法」（`#标题{}` `.布局{}`）。旧的分段制说明 `CPC语言说明.md` 已停用（旧文本仍可导入），勿再喂给 AI。
> ⚠️ 粒度分工不变：**人类手输走「最简」（可省略一切可选参数），AI 生成走「详尽」（写满槽位+半径）**。导入器对两种粒度都零报错地接纳。

---

以下是**要发给 AI 的完整内容**（从 `---BEGIN---` 到 `---END---`）：

---BEGIN---

你是一位角色关系可视化专家。请为一部 ACG 作品 / 小说 / 剧集（作品名见文末【作品】）产出人物关系文件，格式遵循 **CPC 语言**。

这份文件的最终去向是「CP Chart 人物关系图」程序导入。程序是**宽容通配**的：全角半角标点、中文引号、`→`/`➡`/`->` 各种箭头、`×`/`✖` 各种乘号它都认，**你不需要操心标点规范化**。真正会被拒绝的只有三类：**用了没在布局里定义过的角色名**、**原子块里写了关系**、**关系块里写了孤立人名**。所以请专注把内容写对、写全，并把**布局写详尽**（见下）。

## 一、★头号铁律（违反=整条被丢弃，最易犯）

1. **`.布局{}` 是「角色清单的唯一来源」**。任何人想出现在图上，必须先写进 `.布局{}`。
2. **`@关系{}` 和 `!喜好 …{}` 里出现的每一个人名，都必须能在 `.布局{}` 里一字不差地找到。**
   - 最常见的致命错误：写关系时"临时想起"某个人（比如敌对的另一方），直接写进 `@关系{}`，却没把他加进 `.布局{}`。**程序找不到这个人，会报"引用了不存在的角色"并丢弃这一条。**
3. **结尾自检（必做）**：输出前，把 `@关系{}` `!喜好 原子{}` `!喜好 关系{}` 里出现的所有名字逐一与 `.布局{}` 比对。

## 二、★布局写详尽（AI 专属要求，区别于人）

CPC 既支持"极简输入"（人类手输：只写圈号和名单），也支持"详尽输入"。**作为 AI，你要输出详尽粒度**：

1. **每一圈都要写槽位数**，形如 `1[8, 150px]`——方括号里第一项是槽位数（该圈预留多少个位置），**至少等于该圈人数**（想留空位后续加人就写大一点）。
2. **每一圈都要写半径**，形如上例的 `150px`。半径由内向外递增，推荐第 1 圈 `150px`、第 2 圈 `270px`、第 3 圈 `390px`（第 N 圈 = 150 + 120×(N-1)），也可以按角色多寡自由调。圆心（第 0 圈）不写。
3. **方向符自由用，不必转半角**：`A->B`、`A→B`、`A➡B`、`A<-B`（反向）、`A<->B`（双向）、`A*B`（无向）程序都认。
4. 不要因为"不知道能不能写"就省略槽位和半径——**详尽地写**，这才能测出程序的能力边界。

## 三、文件骨架（六块，按此顺序）

```
#标题{
  作品名 角色关系图;
  !填表人: AI;
}

.布局{
  0: 核心角色;
  1[8, 150px]: 角色A, 角色B, 角色C, 角色D, 角色E, 角色F, 角色G, 角色H;
  2[9, 270px]: 角色I, 角色J, 角色K, 角色L, 角色M, 角色N, 角色O, 角色P, 角色Q;
}

@关系{
  爱情: 角色A -> 角色B;
  亲情: 角色C * 角色D;
  敌对: 角色E -> 角色F;
}

!喜好 原子{
  本命: 角色A;
  很喜欢: 角色B, 角色C;
}

!喜好 关系{
  很喜欢: 角色A * 角色B;
}

.样式{
  敌对: relation, #4B8BE2, 2px, 虚线;
}

.图例{
  "关系类型": ["爱情", "亲情", "敌对"],
  "用户喜好": ["本命", "很喜欢"],
  "情感指向": ["情感主体"->"情感对象"]
}
```

## 四、各块怎么写

### `#标题{}`（可选，但建议写）

```
#标题{
  红楼梦 角色关系图;
  !填表人: AI;
  作品: 中国古典文学 > 红楼梦;
  时间: 2026-09;
}
```

- 第一行不带冒号的裸值 = 图的标题。
- 其余为可选字段：`!填表人`（谁填的表）、`作品`（写成 `IP > 作品名`）、`时间`（建表年月）。

### `.布局{}`（角色 + 圆形轨道位置）★必写

- 第 1 行写圆心：`0: 核心角色;`（通常主角，**圆心不写半径**）。
- 其余每圈一行，**详尽写法**：`圈号[槽位数, 半径px]: 角色A, 角色B, ...;`
  - **圈号越小越靠圆心**：第 1 圈 = 与核心最亲近；越外越边缘。
  - 名单**顺序 = 角色在圈上顺时针摆放的次序**（从 12 点方向起），这个顺序是数据，别丢。
  - 分隔符 `,` `，` `、` 都认。
- 例：`2[9, 270px]: 贾母, 王夫人, 贾政, 王熙凤, 贾琏, 薛姨妈, 薛宝琴, 赵姨娘, 贾环;`

### `@关系{}`（角色之间的"关系是什么"）★用定义档词

```
爱情: 角色A -> 角色B, 角色C -> 角色A;
友情: 角色D * 角色E;
```

- 方向符三选一：`A -> B`（单向，A 指向 B）、`A <-> B`（双向）、`A * B`（无向）。
- **本块只用「定义档」词**：`爱情 / 友情 / 亲情 / QPR`。想表达"我多喜欢这对"请去 `!喜好 关系{}`。
- **同一对角色可以出现在多行**（例如既写 `亲情` 又写 `敌对`），会叠加渲染。
- 默认词表达不了的关系（`敌对` `主从` `师生` `宿敌`…）**自建新词**，并在 `.样式{}` 里给它颜色。
- 人名必须来自 `.布局{}`。

### `!喜好 原子{}`（"我多喜欢这个人"，给角色涂色）

```
本命: 角色A;
很喜欢: 角色B, 角色C;
不吃: 角色D;
```

- **本块只放人名，用逗号分隔，不能出现箭头或乘号**（写了会报 E303 并丢弃该条）。
- 强度档词固定四个：`本命 / 很喜欢 / 路好 / 不吃`。**别自创，也别在本块用 `爱情` 这种关系词。**
- 一个人只能有一种喜好（同一人写两次，后者覆盖）。

### `!喜好 关系{}`（"我多喜欢这对关系"）★用强度档词

```
很喜欢: 角色A * 角色B;
雷: 角色C <-> 角色D;
```

- **本块只放关系**，形如 `A * B` 或 `A -> B`（写了孤立人名会报 E304 并丢弃该条）。
- 默认用 `本命 / 很喜欢 / 路好 / 不吃`；也可自建（如 `雷`），并在 `.样式{}` 给颜色。
- `A <-> B` 是**三条独立记录**（一条无向 + 两条有向），程序会各自保存。

### `.样式{}`（可选，只写"偏离默认"的部分）

```
.样式{
  爱情:   #E24B4A;                     /* 只改颜色，其余继承默认 */
  宿敌:   relation, #4B8BE2, 2px, 虚线;  /* 自建词：四字段全给 */
  雷:     favor,    #333333, 4px, 虚线;
}
```

- 格式：`词: 颜色 [, 逻辑, 线宽, 描边];`——**省略的字段自动继承默认值**，不必写全。
- 自建词**必须**在这里出现一次（至少给个颜色），否则图例里它会走灰色兜底。
- 逻辑二选一：`relation`（关系层，细线）/ `favor`（喜好层，粗线）。不写则按预设或按你在哪个块里用它来定。
- 描边中文名：`实线 / 虚线 / 点线 / 双线`。
- ⚠️ **本版本只消费"颜色"**：线宽与描边会被保存，但不改变画出来的样子。照写无妨。

### `.图例{}`（可选）

```
.图例{
  "关系类型": ["爱情", "友情", "敌对"],
  "用户喜好": ["本命", "很喜欢", "雷"],
  "情感指向": ["情感主体"->"情感对象"]
}
```

- 三组：`关系类型`（`@关系` 用到的词）、`用户喜好`（`!喜好` 用到的词）、`情感指向`（箭头含义）。
- 不写也行，程序会照表自动生成前两组。

## 五、默认样式库（程序内置，直接引用，无需声明）

**强度档**（默认粗线，属喜好层 bottom）：

| 词 | 默认颜色 | 语义 |
|---|---|---|
| `本命` | `#d32f2f`（红） | 最爱 |
| `很喜欢` | `#f57c00`（橙） | 很喜欢 |
| `路好` | `#fbc02d`（黄） | 路好 |
| `不吃` | `#222222`（黑） | 不吃这对 |

**定义档**（默认细线，属关系层 top）：

| 词 | 默认颜色 | 语义 |
|---|---|---|
| `爱情` | `#ec407a`（粉） | 爱情 |
| `友情` | `#43a047`（绿） | 友情 |
| `亲情` | `#1976d2`（蓝） | 亲情 |
| `QPR` | `#8e24aa`（紫） | 酷儿柏拉图式关系 |

- 以上 8 词**程序内置**，直接引用即可。
- 需要新关系类型（敌对/主从/师生/宿敌…）→ 造新词并在 `.样式{}` 给颜色。**自建与默认无差别**。

## 六、★不要写的东西（当前版本不支持，写了会被忽略）

- ❌ **逻辑词** `commit` `branch` `merge` `rebase` `init` `reset` `revert`——语法接受但**不渲染**，请勿使用。
- ❌ **`.时间轴{}`**、**`@原子{}`**、**`@组合{}`**、**`!喜好 组合{}`**——当前版本不支持，整块被跳过。
- ❌ 角色人数超过 40、或圈数超过 5 圈——视觉上不可读，请自行收敛。
- ❌ 编造原作品里不存在的角色或关系。

## 七、创作与布局原则

1. 圆心 = 全图视角最核心的人物（主角 / 提问对象）。
2. 第 1 圈 = 与核心关系最紧密的（恋人/挚友/宿敌），越外越疏远。
3. 数量以 8~40 人为宜；人多就多分层。
4. 你能确定的关系才写；不确定就不写。

## 八、输出前必做的四道自检

1. **人名守恒**：`@关系` + `!喜好 原子` + `!喜好 关系` 的所有人名 ⊆ `.布局` 人名。缺一即补。★最关键
2. **无陌生角色**：所有角色名都来自原作品。
3. **布局详尽**：每一圈都带 `[槽位数, 半径px]`，槽位 ≥ 人数。别让任何一圈"裸奔"。
4. **块与内容匹配**：`!喜好 原子` 里只有人名（无箭头）；`@关系` 与 `!喜好 关系` 里只有关系对（无孤立人名）。

## 九、参照样例（AI 详尽版）

> 注意：下面**每个被提到的名字都在 `.布局` 里出现过**，且**每圈都写了槽位和半径**——你的输出必须同样自洽、同样详尽。

```
#标题{
  红楼梦 角色关系图;
  !填表人: AI;
  作品: 中国古典文学 > 红楼梦;
}

.布局{
  0: 贾宝玉;
  1[8, 150px]: 林黛玉, 薛宝钗, 袭人, 晴雯, 史湘云, 紫鹃, 贾探春, 平儿;
  2[9, 270px]: 贾母, 王夫人, 贾政, 王熙凤, 贾琏, 薛姨妈, 薛宝琴, 赵姨娘, 贾环;
}

@关系{
  爱情: 林黛玉 -> 贾宝玉, 贾宝玉 -> 薛宝钗;
  亲情: 贾母 * 贾宝玉, 贾政 * 贾宝玉, 王夫人 * 贾宝玉;
  友情: 贾宝玉 <-> 史湘云, 林黛玉 <-> 紫鹃;
  敌对: 赵姨娘 -> 贾宝玉, 王熙凤 -> 贾环, 贾环 -> 贾宝玉;
}

!喜好 原子{
  本命: 林黛玉;
  很喜欢: 薛宝钗, 史湘云;
}

!喜好 关系{
  很喜欢: 贾宝玉 * 林黛玉;
  雷: 赵姨娘 -> 贾环;
}

.样式{
  敌对: relation, #7B2FBE, 2px, 虚线;
  雷:   favor,    #333333, 4px, 虚线;
}

.图例{
  "关系类型": ["爱情", "亲情", "友情", "敌对"],
  "用户喜好": ["本命", "很喜欢", "雷"],
  "情感指向": ["情感主体"->"情感对象"]
}
```

## 十、【作品】

> 请为作品 **《【替换：填作品名】》** 生成人物关系 CPC 文件。请先想清楚主要角色（8~40 人）、谁居中、分几圈、每圈预留多少槽位、半径多少，再按上面的规则**详尽输出**，最后做第八步自检。

---END---

---

## 使用方法

1. 把 `---BEGIN---` 到 `---END---` 之间**整段**复制。
2. 把最后 `《【替换：填作品名】》` 里的占位改成真实作品名。
3. 发给任何 AI。它应吐出一份块语法 CPC 文本。
4. 把返回文本**原样**贴回 CP Chart 的导入框。若弹错，把错误文案连同一句"请修正后重出"发回给同一个 AI。

## 若报"引用了不存在的角色"

几乎必然是 AI 在关系/喜好里用了布局漏掉的角色。回复 AI 一句：

> 你输出的 `@关系{}`/`!喜好 关系{}` 里有名字没进 `.布局{}`。请把 `.布局{}` 补全到包含所有被提到的角色，再重出一份完整文件。

## 人类最简粒度（仅供人类手输参考，AI 不要这么做）

导入器对**省略**也零报错：不写 `[槽位, 半径]` → 按该圈人数自动均分、半径走默认等距（150/270/390…）。所以人类在导入框手敲时可以写最简：

```
#标题{ 我的CP图 }

.布局{
  0: 斯嘉丽;
  1: 白瑞德, 艾希礼, 梅兰妮;
}

@关系{
  爱情: 斯嘉丽 -> 白瑞德;
}
```

**AI 请勿照此省略**——你的职责是详尽输出、压测程序的通配与布局解析能力。
